name: oft-qwen
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - torch>=2.1.0
    - transformers>=4.40.0
    - peft>=0.10.0
    - trl>=0.8.6
    - datasets>=2.18.0
    - accelerate>=0.28.0
    - bitsandbytes>=0.43.0
    - sentencepiece
    - einops
    - matplotlib
    - numpy
    - tqdm
